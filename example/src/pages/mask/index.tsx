import React from 'react'

import { Mask } from "../../../../src"

export default () => (
  <div style={
    {
      width: 200,
      height: 200,
      background: '#fff',
      position: 'relative',
    }
  }>
    <Mask zIndex={0} />
    dasd
  </div>
)