name: build
on:
  push:
    branches:
      - main
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: install
        run: | 
          yarn
      - name: Build
        run: |
          npm run build
      - name: publish
        run: |
          npm publish

